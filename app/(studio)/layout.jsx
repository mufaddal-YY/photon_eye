export const metadata = {
    title: 'Backend | Photon eye center',
    description: '',
  }
  
  export default function RootLayout({ children }) {
    return (
      <html lang="en">
        <body>{children}</body>
      </html>
    )
  }
  